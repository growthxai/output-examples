# Required for all workflows (LLM provider)
anthropic:
  api_key: "<your-anthropic-api-key>"

# Optional — only needed for specific workflows
# openai:
#   api_key: "<your-openai-api-key>"

# Apollo API key
# apollo:
#   api_key: "<your-apollo-api-key>"

# GitHub token — needed for: changelog_generator, dependency_audit
# github:
#   token: "<your-github-personal-access-token>"

# Beehiiv API key — needed for: ai_hn_digest (newsletter publishing)
# beehiiv:
#   api_key: "<your-beehiiv-api-key>"
